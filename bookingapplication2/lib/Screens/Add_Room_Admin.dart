import 'package:flutter/material.dart';

class AddRoomAdmin extends StatelessWidget {
  const AddRoomAdmin({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
